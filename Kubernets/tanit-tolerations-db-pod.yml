apiVersion: v1
kind: Pod
metadata:
  name: db-pod
spec:
  containers:
  - name: nginx
    image: nginx

  tolerations:
  - key: "type"
    operator: "Equal"
    value: "db"
    effect: "NoSchedule"